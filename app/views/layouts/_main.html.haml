#slider{:data => { :min => 1, :max => @last.id, :value => @last.id}}
#currentDate= @currentTime
#links
  - @links.each do |link|
    .link
      .rank
        = "#{link.rank}. "
      %a.title{:href => link.href}= link.title
      %a.comments{:href => link.comments} Comments